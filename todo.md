# TODO: 고양이가 움직이고 명언도 알려줘요!

> `prd.md` 기준으로 단계별로 나눈 작업 목록입니다. Phase 순서대로 진행하는 것을 권장합니다.

## Phase 0. 프로젝트 셋업
- [x] `prd.md`, `todo.md` 작성
- [x] `CLAUDE.md` 작성 (Claude Code용 프로젝트 안내)
- [x] git 저장소 초기화
- [x] `index.html` / `style.css` / `script.js` 기본 골격 확인
- [x] `assets/` 폴더에 고양이 스프라이트, 배경 이미지, 사운드 리소스 확보 (스프라이트 실제 파일 확보, 배경은 CSS 그라디언트로 대체, 사운드는 Web Audio 합성으로 대체 — 아래 참고)

## Phase 1. 배경 전환 기능
- [x] 도시 배경 / 자연 배경 이미지(또는 CSS) 준비 (CSS 그라디언트로 구현)
- [x] 배경 전환용 버튼(팝업 메뉴 or 화면 내 버튼) UI 구현
- [x] 클릭 시 두 배경 중 하나로 토글되는 로직 구현

## Phase 2. 고양이 등장 및 이동
- [x] 고양이 스프라이트를 화면에 배치
- [x] 화면 내에서 랜덤한 목적지로 걸어 이동하는 로직 구현
- [x] 이동 방향에 따라 스프라이트 좌우 반전 처리

## Phase 3. 고양이 랜덤 정지/그루밍
- [x] 걷기 상태와 정지(가만히 있기) 상태를 랜덤한 주기로 전환
- [x] 정지 상태 중 일정 확률로 "앉아서 그루밍" 애니메이션 재생 (전용 그루밍 스프라이트 없이 idle 이미지 + 흔들림 애니메이션 placeholder)
- [x] 상태 전환 타이머/스케줄러 구현

## Phase 4. 고양이 클릭 상호작용
- [x] 클릭 시 "야옹" 효과음 재생 (음원 파일 없이 Web Audio로 합성한 placeholder 소리)
- [x] 클릭 시 말풍선에 랜덤 사자성어/속담 표시 (`assets/data/sayings.json` 데이터셋 준비)
- [x] 클릭 시 하트 이모티콘 애니메이션 표시
- [x] 위 세 가지 반응 중 매번 랜덤하게 하나 선택되도록 구현

## Phase 5. 상태바 & 배고픔 시스템
- [x] 화면 상단 상태바 UI 구현 (배고픔 / 애정, 각 1~5단계, 숫자 + pip 표시)
- [x] 배고픔 수치 상태 관리 (초기값, 증감 함수)
- [x] 배고픔 3 미만일 때 고양이가 웅크린 채 정지하는 상태 구현
- [x] 참치캔 4개를 화면 랜덤 위치에 항상 유지되도록 스폰 로직 구현
- [x] 참치캔을 고양이에게 드래그하면 배고픔 +1, 참치캔 제거 및 재배치
- [x] 고양이를 드래그해서 이동시킬 때마다 배고픔 -1
- [x] 고양이를 특정 지점(우측 하단 "운세" 지점)으로 드래그하면 오늘의 사자성어/속담 표시
- [x] 실시간 하루 경과 시 배고픔 수치를 1로 리셋하는 로직 (날짜 비교, localStorage 활용)

## Phase 6. 마무리
- [x] `prd.md`의 "확인이 필요한 사항" 항목들 결정 및 반영 (애정 수치 규칙, 속담 데이터 등)
- [ ] 전체 플레이 테스트 (배경 전환 → 이동 → 상호작용 → 상태바 → 시간 리셋) — 정적 서버로 브라우저에서 직접 확인 필요
- [ ] 브라우저 크로스체크 (Chrome/Edge 등)
- [ ] 코드 정리 및 최종 커밋

## Phase 8. 방향키 조작 & 발자국
- [x] 평소엔 랜덤 AI로 돌아다니다가, 방향키(↑↓←→)를 누르면 그 방향으로 직접 조작, 떼면 다시 랜덤 배회로 복귀
- [x] 이동 방향에 따라 스프라이트 좌우 반전 (기존 AI 이동과 동일한 로직 재사용)
- [x] 약 2cm(≈75px) 반경 안에서만 남아있다가 옅어져 사라지는 발자국 흔적

## Phase 9. 실제 로그인 + 온라인 멀티플레이어 (Supabase)
- [x] 로컬(localStorage) 이름 전환 방식을 걷어내고 Supabase 이메일/비밀번호 실제 인증으로 교체 (`config.js`에 프로젝트 URL/anon key 입력 필요, `supabase/schema.sql`을 SQL Editor에서 실행)
- [x] 고양이 저장 데이터(이름/스킨/배고픔/애정)는 `cats` 테이블에 사용자별 RLS(자기 것만 읽기/쓰기)로 저장
- [x] 같은 Supabase 프로젝트로 로그인한 다른 사용자의 고양이를 Presence(실시간, DB에 남지 않는 임시 방송)로 내 화면에 함께 표시 — 위치/이름/스킨 실시간 반영
- [x] Supabase 설정이 안 되어 있으면 로그인 화면에 안내 문구를 보여주고 폼을 비활성화 (게임이 깨지지 않도록)
- [x] 실제 Supabase 프로젝트 생성 + `config.js` 값 입력 + SQL 실행
- [ ] 친구가 접속하려면 정적 파일을 Vercel 등에 배포 (localhost는 본인 컴퓨터에서만 열림)
- [x] 마이크로 발걸음 효과음 직접 녹음(최대 1.5초) → 걸을 때마다 그 소리가 반복 재생되도록, `cats.footstep_sound`에 base64로 저장

## 남은 실제 에셋 (선택, placeholder 교체용)
- [ ] 그루밍 전용 스프라이트 (`assets/sprites/cat-groom.png`)
- [ ] 실제 "야옹"/발걸음 효과음 파일 (`assets/sounds/`, 현재는 Web Audio 합성음)
- [ ] 치즈냥이 / 까망이 전용 스프라이트 (현재는 흰 고양이 스프라이트에 CSS `filter`로 색만 입힌 placeholder)

## Phase 7. 사용자별 프로필 (로그인 시뮬레이션)
- [x] 이름만 입력하는 로컬(localStorage) 로그인 화면 — 서버/비밀번호 없는 시뮬레이션, 사용자 이름별로 데이터 분리 저장
- [x] "최근 사용자" 목록에서 클릭 한 번으로 다른 프로필 전환, "사용자 전환" 버튼으로 로그아웃
- [x] 고양이 이름 설정 → 고양이 발밑에 이름표로 항상 표시
- [x] 고양이 스킨 선택 (새하얀 야옹이 / 치즈냥이 / 까망이) 설정 모달에서 변경, 즉시 반영 + 저장
- [x] 흔들기 감지: 3초 이상 + 이동 경로가 실제 변위보다 훨씬 길게 드래그하면 배고픔/애정이 랜덤(1~3)으로 함께 감소
- [x] 고양이 스프라이트 몸통 불투명 처리 (원본 PNG의 몸통 내부가 알파 0으로 비어 있어 어두운 배경에서 뚫려 보이던 문제 수정)
- [x] 운세 지점(🥠) 클릭으로도 오늘의 한마디를 바로 확인 가능하도록 보완
