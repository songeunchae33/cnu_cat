# PRD: 고양이가 움직이고 명언도 알려줘요!

## 1. 개요

- **프로젝트명**: 고양이가 움직이고 명언도 알려줘요! (가제)
- **한 줄 소개**: 화면 속을 돌아다니는 픽셀아트 고양이를 돌보며(먹이 주기, 쓰다듬기) 랜덤한 속담/사자성어를 받아보는 싱글 페이지 웹 게임
- **플랫폼**: 웹 브라우저 (단일 페이지)
- **기술 스택**: HTML, CSS, JavaScript (프레임워크 없이 바닐라로 진행)

## 2. 유저 시나리오

1. 메인 페이지에 들어가면 흰색 고양이가 화면 위를 자유롭게 돌아다니고 있다.
2. 유저는 고양이를 클릭해 밥을 주거나 쓰다듬을 수 있다.
3. 유저가 고양이를 마우스로 드래그해 특정 지점으로 옮기면, 오늘의 사자성어 또는 속담을 알려준다.
4. 화면 상단 상태바에서 고양이의 배고픔 / 애정 수치를 5단계로 확인할 수 있다.

## 3. 핵심 기능

### 3.1 화면 (배경)
- 도시 배경 / 자연 배경 두 가지를 전환할 수 있다.

### 3.2 고양이 행동
- 앉아서 그루밍하기
- 걷기 (화면 여기저기를 랜덤하게 돌아다님)
- 멈추기 (가만히 정지)
- 위 세 가지 상태를 랜덤한 주기로 반복 전환

### 3.3 상호작용
- **밥 주기**: 화면에 랜덤 배치된 참치캔을 고양이에게 드래그하면 배고픔 수치 +1
- **쓰다듬기 / 놀아주기**: 클릭 시 "야옹" 소리를 내거나, 말풍선으로 사자성어/속담을 보여주거나, 하트 이모티콘을 띄움 (매 클릭마다 랜덤하게 달라짐)
- **드래그 이동**: 고양이를 드래그해서 옮기면 특정 지점 도달 시 오늘의 속담/사자성어 표시, 이동할 때마다 배고픔 수치 -1

### 3.4 상태바
- 화면 상단에 배고픔 / 애정 수치를 각 5단계로 표시
- 배고픔 수치가 3 미만이면 고양이가 몸을 말고 웅크린 채 움직이지 않음
- 배고픔 수치는 실제 시간 기준 하루가 지날 때마다 1로 리셋됨

## 4. 페이지 구성

- 단일 화면(Single Page)으로 구성
- 게임 화면 + 팝업 메뉴(배경 전환, 안내 등)

## 5. 디자인 스펙

- **분위기**: 2D 픽셀아트, 귀엽고 아기자기한 스타일
- **색상**
  - 배경: 파란 하늘 + 흰 구름 (도시) 또는 초록색 자연
  - 고양이: 흰색
- **고양이 스프라이트**: 64x64px, 정지와 이동을 반복하며 화면 곳곳을 이동

## 6. 참고 사이트

- https://tamawebgame.github.io/
- https://ca.nexon.com/Home/Index

## 7. 확인이 필요한 사항 (Open Questions) — 결정 완료

원본 기획에서 구체화되지 않았던 부분을 구현하며 아래와 같이 결정했습니다.

- **애정 수치**: 클릭 반응 중 "하트" 결과가 나올 때만 +1 (최대 5). 별도의 시간 경과 감소는 두지 않음 (배고픔과 달리 PRD에 명시된 감소 규칙이 없어 단순하게 유지).
- **배고픔 0 또는 5일 때의 특수 상태**: 값은 1~5 범위로만 클램프하며(0 없음), 3 미만(1~2)일 때만 웅크림 상태로 처리. 5(최대)일 때 별도 연출은 없음.
- **속담/사자성어 데이터 소스**: `assets/data/sayings.json`에 20개 항목(사자성어/속담 혼합, `text`+`meaning`)을 하드코딩. `fetch` 실패 시(예: `file://`로 직접 열었을 때)를 대비해 `script.js`에 3개짜리 fallback 목록 내장.
- **참치캔이 화면에 4개 유지되는 로직**: 참치캔이 고양이와 겹치는 위치로 드래그되면 즉시 소비되고, 그 즉시 다른 랜덤 위치에 새 참치캔이 스폰되어 항상 4개를 유지.
- **배경 전환 트리거**: 상태바 우측의 "배경 전환" 버튼 클릭 시 도시 ↔ 자연 배경 토글.
- **"특정 지점"으로 드래그 시 오늘의 한마디**: 화면 우측 하단에 고정된 "🥠 운세" 지점을 두고, 고양이를 그 지점으로 드래그해 놓으면 오늘 날짜로 시드된(매일 동일한) 사자성어/속담을 보여줌.

## 8. 범위 외 (Out of Scope, 1차 버전 기준)

- 실제 서버/DB 기반 로그인·인증 (비밀번호, 세션, 보안 등) — 아래 8.1 참고, 이름 기반의 로컬 프로필 전환으로 대체
- 모바일 반응형 최적화
- 사운드/BGM 전체 지원 (야옹/발걸음 소리 등 Web Audio 합성 효과음만 우선)

## 8.1 사용자별 프로필 (로그인 시뮬레이션)

빌드 도구도 서버도 없는 순수 프론트엔드 프로젝트이므로, "로그인"은 실제 인증이 아니라 이름 기반의 로컬(localStorage) 프로필 전환으로 구현했습니다.

- 첫 화면에서 이름만 입력하면(비밀번호 없음) 그 이름으로 된 프로필이 생성/로드됨
- 같은 브라우저에서 여러 이름으로 접속하면 각자 배고픔/애정/고양이 이름/스킨이 독립적으로 저장됨 (로그인 화면의 "최근 사용자" 칩으로 빠르게 전환 가능)
- 상태바의 "사용자 전환" 버튼으로 로그아웃하고 다른 이름으로 다시 시작 가능
- 고양이 이름은 설정 모달에서 지정하며, 고양이 발밑에 항상 이름표로 떠 있음
- 고양이 스킨은 새하얀 야옹이 / 치즈냥이 / 까망이 3종 중 선택 (실제 스프라이트 대신 흰 고양이 원본 이미지에 CSS `filter`로 색을 입힌 placeholder이며, 추후 전용 스프라이트로 교체 가능)
