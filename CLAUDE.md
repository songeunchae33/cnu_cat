# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 프로젝트 안내입니다.

## 프로젝트

"고양이가 움직이고 명언도 알려줘요!" — 화면 속을 돌아다니는 픽셀아트 고양이를 돌보며(먹이 주기, 쓰다듬기) 랜덤한 속담/사자성어를 받아보는 싱글 페이지 웹 게임.

- 기획 문서: [prd.md](prd.md)
- 작업 목록(Phase별): [todo.md](todo.md)
- 원본 초기 기획 메모: [CNU - 강의형_기초웹_PRD.md](CNU%20-%20강의형_기초웹_PRD.md) (참고용, 이후 수정은 `prd.md`에서 진행)

## 기술 스택

- 순수 HTML / CSS / JavaScript (프레임워크, 번들러 없음)
- 빌드 단계 없이 `index.html`을 브라우저에서 바로 열거나 정적 서버로 서빙

## 파일 구조

```
index.html          # 엔트리 포인트
style.css           # 전체 스타일
script.js           # 게임 로직 엔트리 포인트
assets/
  sprites/           # 고양이 스프라이트 (64x64px 픽셀아트)
  backgrounds/        # 배경 이미지 (선택, CSS로 대체 가능)
  sounds/             # 효과음
  data/                # 사자성어/속담 데이터셋 (JSON)
```

## 개발 방식

- `todo.md`의 Phase 순서대로 진행합니다. Phase 하나를 완료하면 해당 체크박스를 갱신하세요.
- 새 기능 추가 시 `script.js`에 바로 이어 작성하거나, 기능이 커지면 `js/` 하위 모듈로 분리해도 됩니다 (현재는 파일 하나로 시작).
- 이미지/사운드 애셋은 실제 파일이 없으면 플레이스홀더(색상 도형, 텍스트)로 우선 동작을 검증하고, 애셋이 준비되면 교체합니다.
- `prd.md`의 "확인이 필요한 사항" 섹션은 구현 중 결정이 필요한 지점입니다. 애매하면 합리적으로 가정하고 PRD에 결정 사항을 반영하세요.

## 실행 / 확인 방법

빌드 도구가 없으므로 정적 서버로 확인합니다. 예:

```bash
npx serve .
# 또는
python -m http.server
```

브라우저에서 열어 배경 전환 → 고양이 이동 → 클릭 상호작용 → 드래그(먹이 주기, 이동) → 상태바 순으로 동작을 확인하세요.
